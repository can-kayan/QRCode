const errorMessages = require("../../../errorHandling/errorMessages")
const { Order, Basked, Product, Scoring } = require("../../../helper/modelsReferance")

const functionDeleteUser={
    Orders:(req,res)=>{
        
    }
}
module.exports={functionDeleteUser}